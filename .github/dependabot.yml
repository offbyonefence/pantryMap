version: 2
updates:
  # Keeps SHA-pinned actions current via PRs (it parses the version comments).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    labels: [ci, dependencies]
